# Final submission handoff — Day 04

## Repository to submit

**https://github.com/ngoclequang1/K4-L3-Day04-LeQuangNgoc-2A202602664-PromptEngineeringToolCalling**

The remote `main` branch was checked during preparation and points to `2902c24ceaa3290059002b78b13c85b339928472`. That commit contains the earlier technical evidence. The final UI/report changes need a new commit and push. This document does not claim that VLearn submission is complete.

## Review entry points

| Deliverable | File |
|---|---|
| Report and technical reflection | [REPORT.md](starter_v0/artifacts/REPORT.md) |
| Roster | [TEAMMATES.md](TEAMMATES.md) |
| Final prompt and tools | [system_prompt.md](starter_v0/artifacts/system_prompt.md), [tools.yaml](starter_v0/artifacts/tools.yaml) |
| Four versions and metrics | [version_log.csv](starter_v0/artifacts/version_log.csv), [experiment review](starter_v0/artifacts/BASELINE-IMPROVEMENTS.md) |
| Team/security evaluation | [team cases](starter_v0/data/eval_group.json), [security review](starter_v0/artifacts/TEAM-SAFETY-EVIDENCE.md) |
| UI and launch instructions | [UI-GUIDE.md](starter_v0/UI-GUIDE.md), [app.py](starter_v0/app.py) |
| Four live transcript scenarios | [capture audit](starter_v0/artifacts/evidence/ui_transcripts/capture_audit.json) |
| Submission file/hash checks | [submission audit](starter_v0/artifacts/evidence/submission_audit.json) |

Base: **21/30 → 23/30 → 27/30 → 29/30**. Team: **9/10**. Adversarial: **6/12**. All six scored runs have zero provider errors. Security failures are disclosed in the report; do not describe the agent as production-safe.

## Prepared locally

- Technical report, solo reflection with disclosed Codex assistance, version evidence, failure analysis, safety review, confirmed solo roster, and four demo scenarios.
- A file manifest and review ZIP generated by `starter_v0/scripts/prepare_submission.py`; it excludes `.git`, `.env`, `.venv`, caches and generated tickets. Curated run/transcript evidence remains included.
- `.gitattributes` preserves exact prompt/tool snapshot bytes so Windows newline conversion does not invalidate recorded artifact hashes when staging these files.
- The review ZIP is a convenience snapshot, **not a replacement for the shared GitHub URL** or contribution history. Regenerate it after final reflection edits.

## Remaining steps for the learner

The solo reflection in REPORT.md C2 is complete, drafted at your request with Codex assistance acknowledged. `TEAMMATES.md` reflects your confirmation that you are the only member.

1. From the repository root, inspect and commit the final files under your Git identity:

   ```powershell
   git status --short
   git diff --check
   git add .
   git diff --cached --stat
   git commit -m "Complete core lab UI, report, and submission evidence"
   git push origin main
   ```

   Review the staged list before committing. The ignore rules exclude local credentials, environment files, scratch outputs and the ZIP. Do not force-add them. Commit your reflection yourself; no commit or push was performed by the assistant in this step.

2. Open the shared GitHub repository and verify the final report, UI, evidence, roster, and your commit are visible. Use the repository root URL above, not a commit/branch/PR link. The existing repository name is retained; the guide recommends a `KX-DAY04-TenNhom` naming pattern, so check course-specific naming expectations before submission.
3. Sign in to your own VLearn account, open Lab Day 04, submit that URL, then reopen the submission to confirm it was saved. This is a solo submission.

Do not mark final checkout complete until those actions have actually happened.

## Demo fallback

Use the four scenarios in REPORT.md A4: normal status, missing asset, corrected asset, and confirmation followed by cancellation. If the model/network is unavailable, open their saved JSON transcripts instead. These scenarios were executed through the live shared session; a personal presentation rehearsal is still up to the learner.
